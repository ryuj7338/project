<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.JobFavoriteRepository">

    <select id="countByMemberIdAndJobPostingId" parameterType="int">
        SELECT COUNT(*) FROM job_favorite
        WHERE memberId = ${memberId}
        AND jobPostingId = ${jobPostingId}
    </select>

    <insert id="insert">
        INSERT INTO job_favorite
        SET memberId = ${memberId},
            jobPostingId = ${jobPostingId},
            regDate = NOW(),
            updateDate = NOW()
    </insert>

    <delete id="delete">
        DELETE FROM job_favorite
        WHERE memberId = ${memberId}
        AND jobPostingId = ${jobPostingId}
    </delete>

    <select id="findFavoriteJobsByMemberId" resultType="com.example.demo.vo.JobPosting">
        SELECT jp.* FROM job_favorite jf
        JOIN job_posting jp
        ON jf.jobPostingId = jp.id
        WHERE jf.memberId = ${memberId}
    </select>

</mapper>